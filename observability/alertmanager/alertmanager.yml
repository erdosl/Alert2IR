global:
  resolve_timeout: 5m

route:
  receiver: lab-null
  group_by:
    - alertname
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

# Stage 2 deliberately has no external notification destination.
receivers:
  - name: lab-null
